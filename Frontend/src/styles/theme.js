/**
 * PURPOSE: Theme configuration object for consistent styling
 * 
 * LOGIC:
 * - Define theme object with:
 *   - colors: Primary, secondary, danger, warning, info, success colors
 *   - breakpoints: Responsive breakpoints (sm, md, lg, xl, 2xl)
 *   - spacing: Spacing scale (4px, 8px, 16px, 24px, 32px, etc.)
 *   - typography: Font families, sizes, weights
 *   - shadows: Box shadow definitions
 *   - borderRadius: Border radius values
 * - Export theme object
 * - Can be used with Material-UI ThemeProvider or styled-components
 * - Ensures consistent design across the application
 * 
 * EXAMPLE:
 * import theme from './theme';
 * <Button style={{ backgroundColor: theme.colors.primary }}>Click</Button>
 */

/**
 * PURPOSE: Re-export ProtectedRoute component for convenience
 * 
 * LOGIC:
 * - Import ProtectedRoute from core/auth
 * - Export as default
 * - Allows importing from routes folder instead of core/auth
 * - Simplifies import paths in other files
 * 
 * EXAMPLE:
 * import ProtectedRoute from './routes/ProtectedRoutes';
 * Instead of: import ProtectedRoute from './core/auth/ProtectedRoute';
 */

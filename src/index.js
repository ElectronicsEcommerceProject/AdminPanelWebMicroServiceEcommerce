/**
 * PURPOSE: Entry point of the React application
 * 
 * LOGIC:
 * - Import React and ReactDOM for rendering
 * - Import main App component
 * - Import global CSS styles
 * - Create root element using ReactDOM.createRoot() targeting 'root' div in index.html
 * - Render App component inside root
 * - Wraps app in React.StrictMode for development warnings
 * 
 * EXAMPLE:
 * ReactDOM.createRoot(document.getElementById('root')).render(<App />)
 */
